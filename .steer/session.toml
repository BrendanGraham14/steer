# Reference to JSON schema for editor support
"$schema" = "../schemas/session.schema.json"

[tool_config]
visibility = "all"

[tool_config.approvals]
tools = []

[tool_config.approvals.bash]
patterns = [
  "*git status*",
  "*git log*",
  "*cargo build*",
  "*cargo check*",
  "*just build*",
  "*just check*",
  "*just test*",
  "*just clippy*",
]
